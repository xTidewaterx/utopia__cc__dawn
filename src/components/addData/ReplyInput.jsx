import React from 'react'

export const ReplyInput = () => {
 
    //short summary
 //write -submit -check object db id - change id property in object to match
 //short summary stop
 //here we need to addData when we "submit" input value and also update the ID property of current db addition to the exact same as the object in the db folder
 //we can then have one that goes underneath and covers both bases under, problem being they must have a relation in same container to use mutual space cross border
 
 
 
 
 
 
 
 
 
    return (
    <div>ReplyInput</div>
  )
}
